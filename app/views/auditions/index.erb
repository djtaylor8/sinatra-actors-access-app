<h1>Auditions</h1>
<p>Get more info by clicking the link, or add audition to profile by checking the box.</p>
<form action="/auditions/<%= @user.slug %>" method="POST">
<input id="hidden" type="hidden" name="_method" value="patch">
<ul>
<% @auditions.each do |audition| %>
    <a href="/auditions/<%= audition.slug %>"><input type="checkbox" name="auditions[]" id="<%= audition.role %>" value= "<%= audition.id %>"<%='checked' if @user.auditions.include?(audition)%>><%= audition.role %> (<%= audition.location %>)</a><br>
<% end %>
</ul>
<input type="submit" value="Register for Audition(s)">
</form>